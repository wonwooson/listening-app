# 프로젝트 작업 규칙

## 코드 수정 전 필수 확인

매 코드 수정 작업을 시작하기 전에 [앱의 의도와 사용 목적](20260919_english/app_purpose_and_intent.md)을 파일에서 직접 읽는다. 이전 대화의 기억이나 요약으로 대신하지 않는다. 새 대화에서도 동일하게 적용한다.

코드 수정에는 기능 추가, 버그 수정, 리팩터링, 화면 변경, 실행 스크립트 변경, 학습 데이터 처리 변경이 포함된다.

1. 목적 문서 전체를 읽는다.
2. 이번 변경과 관련된 목적 문서의 절과 기대 동작을 확인한다.
3. 수정 전에 사용자에게 적용할 기준을 한두 문장으로 설명한다.
4. 작업 중 범위나 구현 방향이 바뀌면 관련 절을 다시 읽는다.
5. 수정 후 실제 결과를 해당 기준과 비교한다.
6. 최종 답변에 목적과의 부합 여부, 검증 결과, 남은 한계를 간단히 적는다.

사용자가 이 대화에서 목적을 명시적으로 변경했다면 최신 사용자 지시를 따른다. 필요한 목적 문서의 변경도 함께 반영한다. 매번 같은 목적을 사용자에게 재확인하거나 승인을 요구하지 않는다.

## 구현 판단 기준

- 핵심 흐름은 구간 청취 → 들린 소리의 한글 기록 → 발음 피드백 → 재청취와 비교다.
- 특정 영상의 개별 수정으로 새 영상에도 적용되는 기능을 구현했다고 보고하지 않는다.
- 시간이나 단어 수에 따른 분할을 AI의 의미 기반 문장·청크 분석으로 설명하지 않는다.
- 전체 처리 요청은 영상 끝까지 확인한다. 앞부분만 처리한 결과를 전체 완료로 보고하지 않는다.
- 저장된 AI 분석 결과, 실행 중 AI 호출, 규칙 기반 처리를 구분한다.
- 자막 기반 발음 가설과 실제 음성 분석을 구분한다. 추정 시간은 정확한 발음 경계로 보고하지 않는다.
- 기존 학습 기록과 원본 자료를 보존한다.

## 목적 문서 관리

목적 문서의 목표를 현재 구현 수준에 맞춰 임의로 낮추지 않는다. 구현이 목표에 못 미치면 그 차이를 명시한다.

구현 상태가 달라지면 목적 문서의 ‘현재 구현과 목표의 차이’도 실제 상태에 맞게 갱신한다. 기능이 추가되었다는 이유만으로 검증되지 않은 목표까지 완료로 표시하지 않는다.

목적 문서를 찾을 수 없으면 먼저 파일 위치를 확인한다. 복구하거나 찾을 수 없다면 목적에 영향을 주는 코드 변경을 시작하지 말고 누락 사실을 알린다.
